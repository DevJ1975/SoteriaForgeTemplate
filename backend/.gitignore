# Amplify Gen 2 — generated / deploy-time artifacts (never committed)
.amplify/
amplify_outputs.json
amplify_outputs.d.ts
amplifyconfiguration.json

# CDK synth output
cdk.out/

# Local secrets / env (only .env.example is committed)
.env
.env.local
.env.*.local

# Build / deps
node_modules/
dist/
*.tsbuildinfo

# Logs / OS cruft
*.log
.DS_Store
