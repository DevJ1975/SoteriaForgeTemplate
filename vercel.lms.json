{
  "buildCommand": "npm run build --workspace @soteria-forge/shared && npm run build --workspace @soteria-forge/lms",
  "outputDirectory": "apps/lms/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
