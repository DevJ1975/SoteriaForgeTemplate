/**
 * Route wrapper for the sign-in screen. Thin by design — implementation lives in
 * src/screens so navigation and UI stay decoupled.
 */
import { SignInScreen } from '../../src/screens';

export default SignInScreen;
