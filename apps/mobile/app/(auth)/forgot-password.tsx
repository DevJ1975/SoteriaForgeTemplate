/**
 * Route wrapper for the forgot-password screen. Thin by design — implementation
 * lives in src/screens so navigation and UI stay decoupled.
 */
import { ForgotPasswordScreen } from '../../src/screens';

export default ForgotPasswordScreen;
