/**
 * @soteria-forge/ui — framework-agnostic design tokens for Soteria FORGE.
 *
 * Consumable by the React Native mobile app and the Vue console alike.
 * The CSS mirror (`tokens.css`) is intentionally NOT re-exported here; web
 * consumers import it directly via `@soteria-forge/ui/tokens.css`.
 */

export * from "./tokens.js";
export * from "./theme.js";

export { tokens, tokens as default } from "./tokens.js";
